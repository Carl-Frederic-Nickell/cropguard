# OpenWeatherMap API Key (required for weather functionality)
# Get your free API key at: https://openweathermap.org/api
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Database Configuration (already configured in docker-compose.yml)
# DATABASE_URL="postgresql://cropguard_user:secure_password_2024@localhost:5434/cropguard_db?schema=public"

# JWT Secret for authentication (already configured in docker-compose.yml)
# JWT_SECRET="backend_jwt_secret_key_2024"

# NextAuth Secret (already configured in docker-compose.yml)
# NEXTAUTH_SECRET="cropguard_secret_key_2024"
# NEXTAUTH_URL="http://localhost:3001"

# Backend URL for API calls (already configured in docker-compose.yml)
# BACKEND_URL="http://localhost:5002"